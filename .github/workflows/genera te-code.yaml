name: Generate Code from Issue
on:
  issues:
    types: [opened, labeled]

jobs:
  generate:
    if: contains(github.event.issue.labels.*.name, 'generate-code')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Generate with Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          # Claude lee el issue
          # Genera código completo
          # Crea un commit automático
      
      - name: Create PR
        uses: peter-evans/create-pull-request@v5
